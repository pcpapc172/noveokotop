# Option A: run scripts/setup-android-sdk.sh to install SDK + generate local.properties automatically.
# Option B: create local.properties manually with your Android SDK path.
sdk.dir=/workspace/android-sdk
